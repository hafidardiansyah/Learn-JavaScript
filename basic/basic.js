// Operator aritmatika (+, -, /, *, %)
var x = 10,
  y = 20;
console.log(x + y);

// Operator pembandingan (<, >, <=, >=, ==, ===, !=, !===)
var kurangDari = x < 20;
console.log(kurangDari);

// Operator penugasan (=, +=, -=, *=, %=)
var unit = "Unit d";
console.log(unit);

// Operator logika (&&, ||, !)
var testUnit = unit != 20 && unit === "Unit d";
console.log(testUnit);

// Type off
typeof true;

// Ternary
console.log(x % 2 == 0 ? "Genap" : "Ganjil");

// String
console.log("Jumlah karakter dari viriabel unit adalah : " + unit.length);

// Boolean
console.log(x != "y");
